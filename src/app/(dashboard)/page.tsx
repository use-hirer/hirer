import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Hirer: Dashboard",
};

export default function DashboardPage() {
  return <div className="font-extrabold text-xl">Home</div>;
}
